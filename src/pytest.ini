[pytest]
# Pytest configuration for ST_8
# Adds src/ to Python path so all modules are importable

pythonpath = .
testpaths = tests
python_files = test_*.py *_test_*.py *_test*.py

markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
